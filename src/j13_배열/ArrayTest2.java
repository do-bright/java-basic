package j13_배열;

public class ArrayTest2 {
    public static void main(String[] args) {
        int [] numbers = new int[5];

        numbers[0]=10;
        numbers[1]=10;
        numbers[2]=10;
        numbers[3]=10;
        numbers[4]=10;


        String[] names = {"도해선", "김정우","홍시영","남주혁"}; // 자동으로 공간 + 초기화 
        // 생성과 동시에 초기화 String 은 바로 생성

        // 배열 늘리려면 어떻게 해야할까 ? 

        // length 는 1부터 시작 Index 는 0부터 시작
        
        // int 랑 int array(int []) 는 아예 다른 것임 
        // 매개변수 넣을 때 유의 !! 
    }
}
